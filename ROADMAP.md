# Roadmap — Portfolio (samylayaida.com)

> Executor contract: each night an unattended agent (`claude -p`, cwd = this repo) picks the FIRST unchecked task, does ONLY that task, verifies it per the task's Verify note, commits with an `autoloop:` prefix (one logical change per commit, never leave the tree dirty), then ticks the checkbox adding the date and a one-line result, and appends details to ## Log. If verification fails: revert, leave unchecked, add a `BLOCKED:` note.

## Context for the executor

- Next.js (App Router) + NextUI + Tailwind + framer-motion (~11.1, already installed). PUBLIC repo; pushing to `main` auto-deploys to samylayaida.com via Vercel.
- Verify baseline for any change: `npm ci` (first run) then `npx tsc --noEmit 2>/dev/null || true` and `npm run build` must succeed. Do not push if the build fails.
- Motion rules: CLAUDE.md "Interaction & motion" + the `interaction-craft` skill. `components/section.tsx` already has `Reveal` + `useReducedMotion` — the house pattern to extend.
- Never add a dependency unattended (framer-motion/NextUI already cover everything below).

## Tasks — interaction & motion (added 2026-07-10, Samy-approved)

- [x] **T01 — Motion tokens + reveal audit** (S) (2026-07-10: added easing/duration CSS vars + global prefers-reduced-motion block to styles/globals.css; replaced the 2 existing transition-all usages with property-specific transitions) — add the interaction-craft easing/duration CSS vars + `prefers-reduced-motion` block to the global stylesheet; sweep existing transitions: replace any `transition-all` and any default `ease` with property-specific transitions using the vars. Verify: build passes; `grep -rn 'transition-all' app components` empty.
- [x] **T02 — Scroll reveals on all sections** (M) (2026-07-11: skills/projects/writing already used Reveal; wrapped contact.tsx's info+form columns (staggered) and the /about Prose block with the existing Reveal component) — apply the existing `Reveal` pattern (whileInView, once, ≤500ms, ease-out, stagger children ~40ms) to every homepage section that lacks it (skills, projects, writing, contact) and to /about. Nothing may delay reading: text must be visible within 500ms of entering the viewport. Verify: build passes; each section wrapped; reduced-motion path renders instantly (Reveal already handles it).
- [x] **T03 — Project cards: hover lift + press** (S) (2026-07-11: added hover -translate-y-0.5+shadow-lg (200ms) and active:scale-[0.97] to FeaturedCard/ProjectCard, the 4 NextUI Button instances (Hero x3, contact submit), and BackToTopButton) — hover `translateY(-2px)` + shadow (200ms), `active:scale-[0.97]` press, on project cards and all buttons/links-as-buttons. Verify: build passes; transform/opacity-only (grep touched files for animated width/height/margin — none).
- [x] **T04 — Micro-detail pass** (M) (2026-07-12: added `.link-underline` scaleX/origin-aware utility to globals.css, applied to inline text links in footer/projects/writing/contact/blog-slug/about; added `filter: blur(2px)→0` to Hero's existing `fade-up` keyframes; added a 200ms colors-only `body` transition for theme switches) — animated link underlines (scaleX from 0, origin-aware for hover direction), subtle blur-in (`filter: blur(2px)→0` + opacity) on the hero intro, smooth theme-switch transition (colors only, 200ms). No infinite loops. Verify: build passes; hero renders instantly under reduced-motion.
- [ ] **T05 — NEEDS-SAMY: Shared-element project detail (blocked on a decision, not on code)** (M) — framer-motion `layoutId` transition from project card → detail. **Question for Samy: do you want a project detail view at all?** The gate ("only if a project detail view exists or Samy adds one") can never be met unattended, because creating that page is inventing a new surface — explicitly out of bounds for the executor. Unblock by either (a) adding a detail view yourself, then dropping the NEEDS-SAMY prefix so the autoloop picks this up, or (b) deleting this task if the single-page portfolio is the intent. Verify (once unblocked): build passes; transition works in dev. (BLOCKED as of 2026-07-13, re-classified 2026-07-15: no project detail view exists — `app/` has only `about`, `blog`, top-level `page.tsx`; no `layoutId` usage anywhere in `app`/`components`.)

## Log
- 2026-07-10: Samy approved unattended autoloop pushes to `main` (= production deploys via Vercel). The build-must-pass verify gate stands.
- 2026-07-10: T01 done — added `--ease-out-custom`, `--ease-in-out-custom`, `--ease-drawer`, `--dur-fast/base/slow` CSS vars and a global `prefers-reduced-motion` block to `styles/globals.css`. Swept `app`/`components` for `transition-all`: found only 2 (`components/BackToTopButton.tsx`, `components/writing.tsx`), replaced both with explicit `transition-[...]` property lists + the new easing vars. No literal default `ease` keyword usage found elsewhere (checked via grep). `npm run build` and `npx tsc --noEmit` pass; `grep -rn 'transition-all' app components` is empty.
- 2026-07-11: T02 done — audit found `components/skills.tsx`, `components/projects.tsx`, and `components/writing.tsx` already wrapped their content in `Reveal`. `components/contact.tsx` and `app/about/page.tsx` lacked it: wrapped contact's info column and form (staggered 0.08s delay) and the /about `Prose` block, all using the existing `Reveal` component (whileInView/once, reduced-motion aware). Ran `npx prettier --write` on touched files to match repo formatting. `npm run build` and `npx tsc --noEmit` pass with no new warnings/errors.
- 2026-07-11: T03 done — added `transition-[...]` property-specific hover/press classes (using `--ease-out-custom`/200ms) to `FeaturedCard` and `ProjectCard` in `components/projects.tsx` (hover -translate-y-0.5 + shadow-lg, active scale-[0.97]), to the 3 NextUI `Button` instances in `components/Hero.tsx` and the submit `Button` in `components/contact.tsx`, and to the native button in `components/BackToTopButton.tsx` (hover -translate-y-1 + shadow-xl, active scale-[0.97], combined with its existing show/hide translate-y). `npm run build` and `npx tsc --noEmit` pass (one pre-existing unrelated `react/jsx-sort-props` warning in Hero.tsx, confirmed present before this change via `git stash`); `git diff` of touched files has no added width/height/margin animation, only transform/box-shadow/opacity/background-color.
- 2026-07-12: T04 done — added a `.link-underline` CSS utility to `styles/globals.css` (`::after` scaleX(0)→1, `transform-origin` flips right→left on hover for direction-aware growth, `var(--dur-base)`/`var(--ease-out-custom)`) and applied it to the inline text links in `components/footer.tsx` (GitHub/LinkedIn/Email), `components/projects.tsx` ("More on GitHub"), `components/writing.tsx` ("All posts"), `components/contact.tsx` (the 3 direct-contact links), `app/blog/[slug]/page.tsx` ("Let me know"), and `app/about/page.tsx` ("Get in touch"); left icon+text and whole-card links (ProjectLinks Code/Live, blog list rows) unchanged since underlining a full block reads oddly. Added `filter: blur(2px)→blur(0)` to the existing Hero `fade-up`/`fade-up-N` keyframes (Hero-only class) and `filter: none` to its reduced-motion override. Added a `body { transition: background-color/color 200ms var(--ease-in-out-custom) }` rule for smooth light/dark theme switches. `npm run build` and `npx tsc --noEmit` pass; touched-file diff only animates `filter`/`transform`/`background-color`/`color` (no width/height/margin); global `prefers-reduced-motion` block (transition-duration/animation-duration → 0.01ms) still covers all new rules, so the hero still renders instantly under reduced motion.
- 2026-07-15: T05 re-classified as NEEDS-SAMY — this is why the autoloop demoted portfolio, and no portfolio code was ever at fault. T05 was the only unchecked task and its title lacked `NEEDS-SAMY`, so `run-roadmaps.sh` counted it as eligible work every night. The executor picked it, correctly found the gate unmet, and reported `blocked` exactly as the task instructs — but the runner treats `blocked` as a failed night (run-roadmaps.sh:182), so a correctly-behaving executor earned a strike on 07-13 and again on 07-15, hitting the two-strike demotion. (07-14 reported `nothing`, which strikes no one.) Verified at re-classification: tree clean, in sync with origin/main, `npx tsc --noEmit` clean and `npm run build` passing (7/7 static pages) — no build failure, deploy error, or broken page exists. Tagging T05 NEEDS-SAMY makes portfolio log "no eligible open tasks, skipping" instead of striking; cleared `state/portfolio.strikes` per the demotion alert's own remediation. Queued as a NEEDS-SAMY task in ~/services/autoloop/ROADMAP.md: the runner cannot distinguish an expected gate-blocked from a real failure, so any future gated task re-creates this trap repo-wide — that fix touches shared infra across 14 roadmaps and is Samy's call.
- 2026-07-13: T05 checked and left unchecked (BLOCKED, expected per gate) — searched `app/` (only `about`, `blog`, `error.tsx`, `layout.tsx`, `page.tsx`, `providers.tsx`) and grepped `app`/`components` for `layoutId`: no project detail view exists and no shared-element transition is in place. Task's own instruction is to leave unchecked with a note rather than invent a new page unattended. No code changes made; working tree otherwise clean. No other unchecked non-NEEDS-SAMY tasks remain in the queue.
